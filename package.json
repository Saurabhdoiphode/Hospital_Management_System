{
  "name": "hospital-management-system",
  "version": "1.0.0",
  "description": "Comprehensive, modern Hospital Management System built with React and Node.js/Express.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "server": "node server/index.js",
    "client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "install-all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "keywords": [
    "hospital",
    "management",
    "react",
    "express",
    "node",
    "mongodb",
    "firebase",
    "koyeb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Saurabhdoiphode/Hospital_Management_System.git"
  }
}
